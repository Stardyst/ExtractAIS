[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "extractais"
version = "2.1.0"
description = "Resumable multi-disk AIS trajectory interval extraction"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "duckdb==1.5.4",
  "PyYAML>=6.0,<7",
  "tqdm>=4.66,<5",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
]

[project.scripts]
extractais = "extractais.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
